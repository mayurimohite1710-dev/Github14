name: My First Workflow

on: push

jobs:
  myfirstjob:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current repo
        uses: actions/checkout@v4

      - name: list files
        run: ls -l